# Generate training set (which is also the test set here)
# note that we do not have access to the original environment so we have modified our boxpusher environment to be close to the original one proposed in SILO

python scripts/abstract_trajectories/silo_obstacle_push/collect_obstacle_push.py