# Generate training set (which is also the test set here)
# note that we do not have access to the original environment so we have modified our blockstacking environment to be close to the original one proposed in SILO

python scripts/abstract_trajectories/silo_pick_and_place/collect_dataset.py